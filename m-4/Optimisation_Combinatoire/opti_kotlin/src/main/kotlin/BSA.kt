import kotlin.random.Random

class BSA {
    private val N: Int = 2
    private val D: Int = 2
    // 5.12 / 5.12
    private val mixRate = 0.1
    private val maxCycle = 2000

    fun initPopulation(){
        for(i in 0..N){
            for(j in 0..D){
                val p = Random.nextDouble(0.toDouble(), 1.toDouble())
            }
        }
    }

    fun run(){
        initPopulation()
        for(c in 1..maxCycle){
            if()
        }
    }
}
